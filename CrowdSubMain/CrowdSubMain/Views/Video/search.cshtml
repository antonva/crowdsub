@model IEnumerable<CrowdSubMain.Models.video>

@{
    ViewBag.Title = "Search";
}

@{
    ViewBag.Message = "for a video profile";
}


<h2>search</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@*<table class="tabe">
    <thead>
        <tr>
            <th>Title</th>
            <th>Type</th>
            <th>Realised</th>
            <th>Subtitle Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.ActionLink(item.video_title, "Profile", new { id = item.id })</td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
        }
    </tbody>
</table>*@

<table class="table">
    <tr>
        <th>
            Results
        </th>
        @*<th>
                Video type
            </th>
            <th>
                Year published
            </th>
            <th>
                Date Created
            </th>
            <th>
                Date updated
            </th>
            <th>
               Description
            </th>
            <th>
                User
            </th>
            <th></th> *@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(item.video_title, "Profile", new { id = item.id }) <!--Link leads to profile for video title-->
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.video_type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.video_year_published)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.video_date_created)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.video_date_updated)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.video_description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.user.user_name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.id })
                </td> *@
        </tr>
    }

</table>
